<Page x:Class="WpfApp1.TestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BgBrush}" Title="Тест">
    <Page.Resources>
        <Style x:Key="OptionRadioButton" TargetType="RadioButton">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="RootBorder"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1.5"
                                CornerRadius="10"
                                Padding="16,12"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="OuterCircle" Width="22" Height="22" CornerRadius="11"
                                        BorderBrush="#CBD5E1" BorderThickness="2"
                                        Margin="0,0,14,0" Background="Transparent">
                                    <Border x:Name="InnerDot" Width="10" Height="10"
                                            CornerRadius="5" Background="Transparent"/>
                                </Border>
                                <ContentPresenter Grid.Column="1"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource PrimaryLighterBrush}"/>
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="InnerDot" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="WrappingTemplate">
            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2,0,2"/>
        </DataTemplate>

        <Storyboard x:Key="BtnEnableAnimation">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                             From="0.95" To="1.0" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                             From="0.95" To="1.0" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="BtnDisableAnimation">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                             To="0.95" Duration="0:0:0.1"/>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                             To="0.95" Duration="0:0:0.1"/>
        </Storyboard>

        <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                    <Setter Property="Background" Value="#A0AEC0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="32">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel>
            <TextBlock Text="🧪 Тест" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
            <TextBlock x:Name="ProgressInfo" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,0,0,24"/>

            <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="16"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    Padding="32" MinHeight="200">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" Opacity="0.04" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="QuestionText" FontSize="17" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap"
                               Margin="0,0,0,20"/>
                    <ItemsControl x:Name="OptionsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton Content="{Binding Text}"
                                             ContentTemplate="{StaticResource WrappingTemplate}"
                                             Style="{StaticResource OptionRadioButton}"
                                             Margin="0,0,0,10"
                                             GroupName="testOptions"
                                             Checked="Option_Checked"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="NextQuestionBtn" Content="Далее →" Style="{StaticResource NavButton}"
                        Height="46" MinWidth="170" Padding="20,10" IsEnabled="False" Click="NextQuestion_Click">
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard Storyboard="{StaticResource BtnDisableAnimation}"/>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>
                <Button x:Name="FinishBtn" Content="Завершить ✓" Style="{StaticResource NavButton}"
                        Height="46" MinWidth="170" Padding="20,10" Margin="12,0,0,0"
                        Background="{DynamicResource SuccessBrush}" Visibility="Collapsed"
                        Click="Finish_Click"/>
            </StackPanel>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>